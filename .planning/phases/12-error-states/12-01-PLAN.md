---
phase: 12-error-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/content.js
autonomous: true
requirements:
  - ERR-01
  - ERR-02

must_haves:
  truths:
    - "When CDN fetch fails and stale cache is used, the debug panel shows an amber warning"
    - "When CDN fetch fails with no cache, the existing red error line in the debug panel is unchanged"
    - "When a fresh fetch succeeds, both profileStaleWarning and profileLoadError are cleared"
    - "The get-game message response carries profileLoadError and profileStaleWarning"
  artifacts:
    - path: "extension/content.js"
      provides: "profileStaleWarning variable, stale-branch wiring, debug panel amber line, get-game response fields"
      contains: "profileStaleWarning"
  key_links:
    - from: "fetchAndCacheProfile() stale-cache branch"
      to: "profileStaleWarning variable"
      via: "assignment inside the usedStale=true branch"
      pattern: "profileStaleWarning = err"
    - from: "updateDebugPanelStatus()"
      to: "profileStaleWarning"
      via: "lines.push amber span"
      pattern: "f5b000.*profileStaleWarning|profileStaleWarning.*f5b000"
    - from: "get-game message handler (line 3511)"
      to: "profileLoadError, profileStaleWarning"
      via: "sendResponse object"
      pattern: "profileLoadError.*profileStaleWarning"
---

<objective>
Wire profileStaleWarning and profileLoadError into the debug panel and the get-game message
response in content.js.

Purpose: The stale-cache path in fetchAndCacheProfile() currently fails silently. This plan
makes that warning visible in the debug panel (amber) and exposes both error flags to the
popup via the existing get-game message so Plan 02 can render them near the profile selector.

Output: content.js with profileStaleWarning declared, set in the stale-cache branch, cleared
on fresh success, rendered as an amber line in updateDebugPanelStatus(), and returned in the
get-game sendResponse call.
</objective>

<execution_context>
@C:/Users/spick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ClaudeCodeProjects/twitch-overlay/.planning/ROADMAP.md
@C:/ClaudeCodeProjects/twitch-overlay/.planning/STATE.md
@C:/ClaudeCodeProjects/twitch-overlay/.planning/phases/12-error-states/12-CONTEXT.md

<interfaces>
From extension/content.js (current state — executor must re-read before editing):

Line 93:  let profileLoadError = null; // null | string
Line 559: profileLoadError = null;       // success path clear
Line 573: profileLoadError = err.message; // no-cache failure
Lines 563-571: catch block — stale-cache branch sets usedStale=true
Lines 2625-2648: updateDebugPanelStatus() — lines[] array, innerHTML at end
Line 3511: if (msg && msg.type === "get-game") { sendResponse({ game: detectedGame }); }

Debug panel line pattern (lines 2632-2636):
  lines.push(`span style="color:#ff5c5c" -- profile error: ${profileLoadError} -- /span`);
  (use same pattern for amber stale-warning line with color #f5b000)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare profileStaleWarning and wire it in fetchAndCacheProfile</name>

  <read_first>
    extension/content.js lines 85-100 (variable declarations — profileLoadError on line 93)
    extension/content.js lines 547-577 (fetchAndCacheProfile full body)
  </read_first>

  <action>
    Three edits to extension/content.js:

    Edit A — Variable declaration (after line 93):
    Add immediately after the `let profileLoadError = null;` line:
    `  let profileStaleWarning = null; // null | string — CDN unreachable but stale cache used`

    Edit B — Success path clear (line 559 area):
    After `profileLoadError = null;` in the try-block success path, add on the next line:
    `      profileStaleWarning = null;`

    Edit C — Stale-cache branch set (lines 566-570 area):
    Inside the inner try inside the catch, after the `applyProfile(cached.profile, ap.url);`
    call and before `usedStale = true;`, insert:
    `          profileStaleWarning = err.message;`

    Do NOT add profileStaleWarning to the no-cache failure branch (lines 572-575) — that
    branch sets profileLoadError, which is already handled. The two variables are mutually
    exclusive per fetch attempt.
  </action>

  <verify>
    node -e "const s=require('fs').readFileSync('extension/content.js','utf8'); const ok=s.includes('let profileStaleWarning = null') && s.includes('profileStaleWarning = null') && s.includes('profileStaleWarning = err.message'); console.log(ok?'PASS':'FAIL'); process.exit(ok?0:1);"
  </verify>

  <acceptance_criteria>
    - `grep -n "profileStaleWarning" extension/content.js` returns at least 3 lines: the declaration, the clear on success, and the set in stale branch
    - The declaration line contains the comment "CDN unreachable but stale cache used"
    - The clear line (`profileStaleWarning = null`) is inside the same try-block as `profileLoadError = null` (i.e. the success path)
    - The set line (`profileStaleWarning = err.message`) is inside the inner try of the catch block, before the `usedStale = true` line
  </acceptance_criteria>

  <done>profileStaleWarning is declared, set on stale-cache fallback, and cleared on fresh success</done>
</task>

<task type="auto">
  <name>Task 2: Render profileStaleWarning in debug panel and expose both flags in get-game response</name>

  <read_first>
    extension/content.js lines 2625-2648 (updateDebugPanelStatus)
    extension/content.js line 3511 (get-game message handler)
  </read_first>

  <action>
    Two edits to extension/content.js:

    Edit A — Debug panel amber line (inside updateDebugPanelStatus, after the existing
    profileLoadError red-line block at lines 2635-2637):
    Add a parallel block immediately after the closing brace of the profileLoadError if:

      if (profileStaleWarning) {
        lines.push(`span style="color:#f5b000" -- WARNING CDN unreachable — using cached profile (${profileStaleWarning}) -- /span`);
      }

    (Replace -- with actual angle-bracket tags in the template literal; do not use fenced
    code blocks here. The span tag pattern mirrors the existing profileLoadError line.)

    Edit B — get-game response (line 3511):
    Change the sendResponse call from:
      sendResponse({ game: detectedGame });
    to:
      sendResponse({ game: detectedGame, profileLoadError: profileLoadError, profileStaleWarning: profileStaleWarning });

    No other changes. Do not restructure the if-statement or add return.
  </action>

  <verify>
    node -e "const s=require('fs').readFileSync('extension/content.js','utf8'); const panel=s.includes('profileStaleWarning') && s.includes('f5b000') && s.includes('CDN unreachable'); const resp=s.includes('profileLoadError: profileLoadError') && s.includes('profileStaleWarning: profileStaleWarning'); console.log('panel:',panel,'resp:',resp); process.exit(panel&&resp?0:1);"
  </verify>

  <acceptance_criteria>
    - `grep -n "CDN unreachable" extension/content.js` returns exactly 1 line inside updateDebugPanelStatus
    - That line uses color `#f5b000` (amber), not `#ff5c5c` (red)
    - The warning message text contains "using cached profile"
    - `grep -n "profileStaleWarning" extension/content.js` now returns at least 4 lines (declaration + clear + set + panel render + get-game response = 5)
    - `grep -n "get-game" extension/content.js` returns a line whose sendResponse includes both `profileLoadError:` and `profileStaleWarning:` fields
    - The get-game handler line does NOT use innerHTML or construct DOM — it only calls sendResponse with a plain object
  </acceptance_criteria>

  <done>
    Debug panel shows amber "CDN unreachable — using cached profile" when stale cache is active.
    get-game response object includes profileLoadError and profileStaleWarning fields.
    Popup can now read these in its get-game response handler (wired in Plan 02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN response -> content.js | Untrusted network data enters err.message, which is a JS Error string from fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | profileStaleWarning rendered via innerHTML in debug panel | mitigate | Existing pattern: debug panel innerHTML is developer-facing only (no viewer-supplied content). err.message from fetch is a JS runtime string, not profile-supplied content. Acceptable. |
| T-12-02 | Spoofing | get-game response fields read by popup | accept | Response travels via chrome.tabs.sendMessage within the same extension — no external trust boundary. |
</threat_model>

<verification>
After both tasks complete:

1. Open a Twitch stream page with the extension loaded.
2. Open DevTools, filter console by `[overlay`.
3. In DevTools > Application > Local Storage, delete the cached profile entry
   (key matching `streamGenie_profile_*`).
4. In DevTools > Network, block the raw.githubusercontent.com request for profile.json.
5. Reload the Twitch page.
6. Open the debug panel (Alt+Shift+D or via popup).
7. Expected: amber line reading "CDN unreachable — using cached profile" if stale data
   was previously cached; red "profile error" line if no cache exists at all.
8. Unblock the network request, reload — both warning lines should disappear.
</verification>

<success_criteria>
- profileStaleWarning is null when profile loads fresh from CDN
- profileStaleWarning is a non-null string when CDN fetch fails and stale cache is used
- profileLoadError is null in the stale-cache case (the two flags are mutually exclusive)
- Debug panel amber line appears when profileStaleWarning is set, is absent otherwise
- Existing red profileLoadError line in debug panel is unchanged
- get-game sendResponse includes profileLoadError and profileStaleWarning fields
</success_criteria>

<output>
After completion, create .planning/phases/12-error-states/12-01-SUMMARY.md
</output>
