---
context: phase
phase: 13-privacy-permissions-disclosure
task: complete
total_tasks: 3
status: complete
last_updated: 2026-05-16T00:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints. Phase 13 is fully complete._

<current_state>
Phase 13 (Privacy & Permissions Disclosure) is done. All v1.0 Beta phases (10–13) are
shipped and verified. The project is at the Chrome Web Store submission step.

CR-01 from the code review was a false alarm — config.js has never been committed,
is gitignored, and the secret is safe. No action needed there.
</current_state>

<completed_work>

Phase 13 — all tasks complete, verified, committed:
- Task 1: "Privacy →" link added to first-run banner in popup.html (href, target=_blank, rel=noopener noreferrer, color:#c9b3ff)
- Task 2: ## Permissions section added to README after ## How it works — six-row table matching manifest
- Task 3: STORE-LISTING.md created at repo root with short desc (124 chars), full desc with privacy statement, permissions justification table
- Code review ran; 4 Phase-13 findings fixed (char counts, cdn.jsdelivr.net description, privacy claim wording)
- VERIFICATION passed 3/3 must-haves; PRIV-01 and PRIV-02 satisfied
- 4 Playwright e2e tests added (privacy-disclosure.spec.js); full suite 10/10 passing

Commits this session: 8ea13a7 → 0f50b73
</completed_work>

<remaining_work>

Pre-submission cleanup (not planned phases — just housekeeping):

1. **Fix manifest title** — `extension/manifest.json` `action.default_title` still says
   "Stream Overlay" instead of "Stream Genie". One-line edit.

2. **Clean up root-level noise files** — BRANCH_SUMMARY.md, CLEANUP_INSTRUCTIONS.md,
   ISSUE_ANALYSIS.md, ISSUE_SUMMARY.md are untracked at repo root. Delete or gitignore
   before repo goes public.

3. **Verify privacy policy URL is live** — https://frothydv.github.io/streamGenie/privacy
   is linked from the banner and STORE-LISTING. Confirm the page actually loads.

4. **Package and submit to CWS** — zip the extension/ folder, paste from STORE-LISTING.md.

Post-beta roadmap (all deferred):
- Auto-profile selection in popup
- Creator/streamer profile config (creators/twitch/ lookup)
- Rate limiting on Cloudflare Worker
- Profile upvotes + sorting
- YouTube + Firefox support
</remaining_work>

<decisions_made>

- cdn.jsdelivr.net permission description changed from "Fetch the game catalog" to
  "CDN fallback for community profiles" — catalog uses raw.githubusercontent.com directly;
  jsDelivr is only a URL-rewrite fallback via ensureRawUrl()
- CR-01 (hardcoded secret): confirmed false alarm — config.js never committed, already gitignored
- 4 Playwright tests chosen over a doc linter — popup regressions are more likely than doc edits
</decisions_made>

<blockers>

None. All must-haves are shipped. Remaining items are pre-submission housekeeping.
</blockers>

## Required Reading (in order)

1. `STORE-LISTING.md` — CWS submission copy, ready to paste
2. `extension/manifest.json` — needs `action.default_title` fix before submission
3. `.planning/phases/13-privacy-permissions-disclosure/13-VERIFICATION.md` — full verification report

## Infrastructure State

- Extension: v0.9.2, loads cleanly at chrome://extensions/
- Playwright suite: 10/10 passing (6 error-states + 4 privacy-disclosure)
- Profiles repo: frothydv/streamGenieProfiles — active, STS2 community profile seeded
- Cloudflare Worker: streamgenie-submit.vbjosh.workers.dev — deployed
- config.js: present on disk, gitignored, never committed — secret is safe

<context>
Session completed Phase 13 in full. The v1.0 Beta milestone is done. The CWS submission
path is clear: fix the manifest title, clean up 4 stray markdown files at root, verify
the privacy policy URL loads, then package and submit. STORE-LISTING.md has all four
required CWS fields ready to paste.
</context>

<next_action>
Start with: fix manifest.json action.default_title ("Stream Overlay" → "Stream Genie"),
then delete the 4 stray root-level markdown files (BRANCH_SUMMARY.md, CLEANUP_INSTRUCTIONS.md,
ISSUE_ANALYSIS.md, ISSUE_SUMMARY.md), then verify https://frothydv.github.io/streamGenie/privacy loads.
</next_action>
